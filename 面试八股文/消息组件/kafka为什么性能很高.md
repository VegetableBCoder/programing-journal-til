# kafka为什么性能很高

* IO模型: Reactor, 多路复用
* 文件顺序写入
* 0-copy
  * 不把内容加载到jvm, 只拿描述符, 直接让操作系统根据描述符把buffer丢去网卡发送
* 分布式架构方式, 每个节点都负责一部分分区, 扩展节点数和分区数可以提升整体的吞吐
* 消息批量收发的支持; 消息的收发不是以消息为单位, 生产者可以通过消息累加器将多条消息打包到一个batch放到队列等发送线程去发送; 消费者拉消息时leader也会读取RecordBatch, 尽量在满足size限制的情况下多返回消息
* 大量使用页缓存, mmap, 将文件和虚拟内存映射
* 