# redis快的原因

* 首先, redis的数据在运行时都在内存中, 排除虚拟内存的IO外, 读写操作不直接需要磁盘交互, 另外虚拟内存的问题可以通过操作系统配置来限制redis进程使用swap
* redis使用的单线程模型, 只有一个线程处理事件循环, 不需要上下文切换
* redis使用多路复用的IO, 虽然引入了多线程IO, 但是这些线程只负责产生文件事件, 处理时间循环的仍然只有一个线程
