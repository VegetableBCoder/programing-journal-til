# 为什么用跳表

* redis在zset中使用字典和hash两种数据结构结合来实现zset
* 跳表对标的是红黑树, 与红黑树相比跳表的查找平均复杂度都是O(logN)但是实现更简单
  * zset需要实现的range 查询在红黑树上查找需要O(N+k), 或者维护子树规模来实现O(logN+k), 跳表维护了节点之间的距离, 本来就支持O(logN+k)
  * 使用zrangebyscore的话复杂度也是也一样是O(logN+k), 但是复杂度要高很多
* 总的来看红黑树更适合单点查找, 跳表天然对范围查找很友好