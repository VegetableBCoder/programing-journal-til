# redis cluster集群扩容怎么数据平滑过度，从客户端设计

* 没搞明白这问题问的是啥

* 客户端会从服务端拿节点和槽的绑定关系
* 扩容的时候服务端将一部分槽迁到新的节点, 客户端访问正在迁移的槽会触发ASK错误, 临时将请求定位到新的槽上去
* 如果槽已经迁移完了, 客户端的绑定信息还没有更新, 那么会触发MOVED错误, 客户端后续访问这个槽都会去新的节点
* moved错误只是被动纠错机制, 客户端还会主动刷新绑定关系