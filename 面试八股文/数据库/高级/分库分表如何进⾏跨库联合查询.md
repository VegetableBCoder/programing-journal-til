# 分库分表后的连表查询

* 能别join吗?? 自己到代码里面去做扇出, 关联的逻辑好不好, 球球了...
* 小表join可以考虑全局表的思路, 每个节点都存一份
* 同库join这种还好, 能确认是在同一个库的可以去join
* 如果要跨库join得中间层参与, 可能得不偿失
* 做报表之类的建议倒到数仓宽表去做

## 订单表按照user_id分片, 但是想直接用id查询怎么办?

* 基因法, 想办法把一致性hash的结果, 或者把hash信息塞到订单id里面去
  * 适合用雪花id的变种, 从数据库加载序号段, 然后时间戳和序号中间塞基因信息
* 映射表法: 前提是不是给用户端用的, 而是后台用的, 慢点没啥问题; 搞一个映射表, 先把userId都查出来, 然后再查
  * 理想的场景不是这里, 而是订单想按商户维度统计, 先查询商户的用户id, 然后按user_id+商家id到订单表去搜
* 广播查询: 广播给所有有分片的节点;
* 跟ES搜索玩去吧...