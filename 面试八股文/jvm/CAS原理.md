# CAS原理

## 在内存操作变量的时候

* 通过CPU的单条指令来实现检查和设置值, java里面是通过Unsafe类直接调用相关指令的
* 需要注意的是一般需要配置volatile来控制变量的线程之间的可见性
  
## Redis实现

* 由于redis天然是单线程的, 所以只要执行lua脚本就行了

## 数据库实现

* 通过行锁锁住要更新的行, 然后通过where检测行是否满足原值条件 然后更新为新值